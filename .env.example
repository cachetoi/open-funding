# OpenFunding currently requires no secrets for its built-in public sources.
# Future providers that require credentials should be configured here as
# environment variable names only. Never commit the real values.
# EXAMPLE_PROVIDER_API_KEY=
